/* The word count program could be tested by creating a collection of files
 * with known line, word, and character counts and asserting that the program
 * returns the expected results. Input that might catch bugs include corner
 * cases, or input causing conditionals to not function as intended. */
